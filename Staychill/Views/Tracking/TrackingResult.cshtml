@* @model Staychill.Models.ProductModel.TrackingModel.Tracking

@{
    ViewData["Title"] = "Tracking Result";
}

<div class="container">
    <h1>Tracking Result</h1>

    <div class="alert alert-success">
        <h4>Your shipment has been created successfully!</h4>
        <p><strong>Shipment Code:</strong> @Model.ShipmentCode</p>
        <p><strong>Status:</strong> @Model.Status</p>
    </div>

    <h2>Cart Item Details</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Product Name</th>
                <th>Quantity</th>
                <th>Unit Price</th>
                <th>Total Price</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Model.Cart.Product.ProductName</td>
                <td>@Model.Cart.Quantity</td>
                <td>@Model.Cart.UnitPrice</td>
                <td>@Model.Cart.TotalPrice</td>
            </tr>
        </tbody>
    </table>
</div>
 *@